/* ── MotorMind Report JS ── */

/* Cetak halaman */
function doPrint() {
  window.print();
}

/* Tutup popup */
function doClose() {
  window.close();
}

/* Download CSV — redirect ke endpoint */
function downloadCSV(url) {
  window.location.href = url;
}
